replicaCount: 1

image:
  repository: ghcr.io/davidnet-net/docs
  pullPolicy: Always
  tag: "latest"

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: true
  host: docs.davidnet.net
  ingressClass: traefik
